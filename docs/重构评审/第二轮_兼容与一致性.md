# 第二轮：兼容与一致性评审

## 已完成

- `application.backtest` 保持唯一公开回测编排入口。
- `strategy_backtest` 与 `BacktestService` 保留一版弃用兼容，不做破坏性删除。
- 每日与回测制品写入器保持各自既有公开目录和哈希契约。
- v3 对象存储继续承担历史读取，不再静默替换公开写入格式。

## Review 发现与修复

- 回测写入器原先绕到 `storage.制品.backtest_publish`，返回对象哈希路径而非 v2 报告路径。
- 已恢复 `BacktestArtifactWriter.export_v2` 为公开写入实现，与每日制品修复保持一致。

## 验证

- 旧入口、弃用警告、每日/回测制品和 Golden Master：62 项通过。

## 遗留风险与准入结论

- v3 历史读取仍依赖旧存储模块，必须等迁移器与真实历史样本对账后才能移除。
- 准入第三轮：通过。
