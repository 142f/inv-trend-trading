# 根据我现阶段的代码优化一下下面的内容：收到，非常感谢您的详细指正。您提到的**“输入构建（Audio
_Exported on 04/22/2026 at 17:13:45 GMT+8 from Alibaba Lingma via WayLog_


**User**

根据我现阶段的代码优化一下下面的内容：收到，非常感谢您的详细指正。您提到的**“输入构建（Audio在上，Video在下）”**是LipFD模型中非常关键的数据预处理细节，这直接对应论文图4（Figure 4）左侧的输入数据流。
根据您的最新要求，我将权利要求书重新细化为6个步骤，并在**步骤1（数据预处理）**中详细描述“声谱图与视频帧拼接”的复合图像构建过程。
以下是修改后的第二部分：权利要求书。
第二部分：权利要求书
1、一种基于多模态融合的人脸深度伪造检测技术的方法，其特征在于，所述方法首先构建包含视听信息的复合输入图像，将提取的音频声谱图置于图像上方，将连续的视频帧序列置于图像下方，形成时空对齐的统一表征；随后对下方的视频帧进行多尺度裁剪，分别获取头部、面部和唇部区域序列；利用全局特征编码器处理复合输入图像以提取视听时序不一致性，同时利用全局-区域编码器提取各局部区域的细微伪造痕迹；通过区域感知模块计算各区域的动态权重，模拟人类对唇部运动与声音信号之间生物学联系的认知过程；最后结合区域感知损失与分类损失进行联合优化，实现对唇形同步伪造视频的精准检测；该方法的具体步骤包括：
步骤1：视听复合输入图像的构建，该步骤包括：
步骤11： 获取待检测视频片段，设定时间窗口大小为
        TTT
      
（例如选取5帧），提取该窗口内的连续视频帧序列以及对应的音频信号；
步骤12： 对音频信号进行短时傅里叶变换（STFT）生成对应的音频声谱图（Spectrogram），作为音频模态的视觉化表征；
步骤13： 构建单张复合输入图像
        III
      
，该图像在空间布局上分为上下两部分：上方区域放置步骤12生成的音频声谱图，下方区域水平排列步骤11选取的
        TTT
      
帧视频图像；
步骤14： 对该复合输入图像
        III
      
进行归一化处理，使其成为蕴含视听双模态信息的统一张量，作为后续网络的全局输入；

步骤2：视频帧的多尺度区域裁剪，该步骤包括：
步骤21： 针对步骤13中位于复合图像下方的每一帧视频图像，利用人脸检测算法定位人脸中心；
步骤22： 基于人脸中心进行不同比例的裁剪，生成三个不同尺度的图像序列：包含背景与头部姿态的头部区域序列 (
        chc_hch​
      
)、聚焦面部整体特征的面部区域序列 (
        cfc_fcf​
      
)、以及聚焦嘴部细微运动的唇部区域序列 (
        clc_lcl​
      
)；
步骤23： 将上述三个裁剪序列调整至统一分辨率，形成局部视觉输入流
        C={ch,cf,cl}C=\{c_h, c_f, c_l\}C={ch​,cf​,cl​}
      
；

步骤3：全局特征编码与时序关联挖掘，该步骤包括：
步骤31： 将步骤1得到的复合输入图像
        III
      
输入到预训练的视觉Transformer（ViT）中作为全局特征编码器（
        EGE_GEG​
      
）；
步骤32： 利用ViT的自注意力机制（Self-Attention），在复合图像内部建立上方音频声谱图与下方视频帧序列之间的长距离依赖关系，捕捉唇动与声音在时间维度上的相关性；
步骤33： 输出包含全局视听上下文信息的全局特征向量
        FG=ViT(Conv(I))F_G = ViT(Conv(I))FG​=ViT(Conv(I))
      
；

步骤4：全局-区域特征的融合编码，该步骤包括：
步骤41： 构建全局-区域编码器（
        EGRE_{GR}EGR​
      
），其输入包括步骤2得到的局部裁剪序列
        CCC
      
和步骤3得到的全局特征向量
        FGF_GFG​
      
；
步骤42： 对于每一个裁剪区域（
        ch,cf,clc_h, c_f, c_lch​,cf​,cl​
      
），将其图像特征与全局特征
        FGF_GFG​
      
进行拼接，使局部特征能够感知全局的时序上下文；
步骤43： 通过编码器处理拼接后的特征，输出融合了时序信息与局部细节的区域特征
        FRF_RFR​
      
；

步骤5：基于区域感知模块的动态权重分配，该步骤包括：
步骤51： 引入区域感知模块（Region Awareness），该模块由全连接层和Sigmoid激活函数构成；
步骤52： 将步骤4得到的各区域特征
        FRF_RFR​
      
输入该模块，计算每个区域在当前时间窗口下的重要性权重
        ω\omegaω
      
；
步骤53： 根据权重
        ω\omegaω
      
对全局特征
        FGF_GFG​
      
与各区域特征
        FRF_RFR​
      
进行加权融合，生成最终的判别特征
        FFF
      
，确保模型自动聚焦于如唇部等高频篡改区域，而忽略背景噪声；

步骤6：联合损失函数的构建与模型优化，该步骤包括：
步骤61： 将加权融合后的特征
        FFF
      
输入分类器，输出真伪预测结果；
步骤62： 构建分类损失函数
        Lcls\mathcal{L}_{cls}Lcls​
      
，用于衡量预测标签与真实标签的差异；
步骤63： 构建区域感知损失函数
        LRA\mathcal{L}_{RA}LRA​
      
，通过指数函数最大化唇部/面部区域与头部背景区域的权重差值，强制模型关注生物学特征不一致的区域；
步骤64： 采用公式
        min⁡ω⋅LRA+Lcls\min \omega \cdot \mathcal{L}_{RA} + \mathcal{L}_{cls}minω⋅LRA​+Lcls​
      
对模型进行端到端训练。

2、根据权利要求1所述的一种基于多模态融合的人脸深度伪造检测技术的方法，其特征在于，在步骤13中，所述复合输入图像
        III
      
的构建方式是将音频声谱图在通道维度或空间维度上与视频帧序列进行拼接，使得音频的频谱能量变化与视频的唇部运动变化在同一张量空间内呈现对应关系。

3、根据权利要求1所述的一种基于多模态融合的人脸深度伪造检测技术的方法，其特征在于，在步骤22中，所述多尺度裁剪的具体比例设置为：头部区域序列 (
        chc_hch​
      
) 的裁剪框大小为原人脸检测框的1.0倍，面部区域序列 (
        cfc_fcf​
      
) 为0.65倍，唇部区域序列 (
        clc_lcl​
      
) 为0.45倍。

4、根据权利要求1所述的一种基于多模态融合的人脸深度伪造检测技术的方法，其特征在于，在步骤31中，所述全局特征编码器采用在CLIP（Contrastive Language-Image Pre-training）数据集上预训练的ViT-L/14模型，以利用其在大规模数据上习得的强鲁棒性视觉表征能力。
5、根据权利要求1所述的一种基于多模态融合的人脸深度伪造检测技术的方法，其特征在于，在步骤63中，所述区域感知损失函数
        LRA\mathcal{L}_{RA}LRA​
      
的数学表达式为：
        LRA=∑∑k⋅exp⁡([ωji]max−[ωji]h)\mathcal{L}_{RA} = \sum \sum k \cdot \exp([\omega^i_j]_{max} - [\omega^i_j]_h)LRA​=∑∑k⋅exp([ωji​]max​−[ωji​]h​)
      
，其中
        [ωji]max[\omega^i_j]_{max}[ωji​]max​
      
代表当前特征栈中的最大权重值，
        [ωji]h[\omega^i_j]_h[ωji​]h​
      
代表未裁剪的头部/背景区域权重值，
        kkk
      
为超参数，旨在增大关键区域与背景区域的关注度差异。


---


**Alibaba Lingma**

用户请求对权利要求书内容进行优化，重点在于视听复合输入图像的构建、多尺度区域裁剪、全局特征编码、区域感知权重分配以及联合损失函数的设计。当前轮次由common agent处理，回答详细列出了优化要点，并提供了结构更清晰、技术细节更明确的优化后权利要求书，包含独立权利要求和从属权利要求，补充了参数配置、实现方式及适用场景等信息。

---
