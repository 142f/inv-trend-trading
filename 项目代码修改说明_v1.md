# inv-trend-trading P0 执行决策与通知修复说明 v1

## 1. 交付信息

| 项目 | 内容 |
|---|---|
| 原始基线 | `inv-trend-trading-core-architecture-20260823.zip` |
| 设计依据 | `项目完整设计审计报告_v1.md`、`CORE_CODE_ARCHITECTURE.md` |
| 修改版本 | `0.2.1` |
| 实施范围 | P0：正式通知事件与最终入场决策统一；资格未确认 fail closed；恢复兼容；定向回归测试 |
| 未实施范围 | P1/P2：统一 daily/detector/backtest 策略内核、配置单一事实源、依赖反转、策略 challenger、复杂 regime |
| 数据库迁移 | 不需要 |
| CLI 变更 | 无 |
| 策略参数变更 | 无；仅修正动作与通知语义 |

本次没有进行大规模重构。保留现有六阶段工作流、指标计算、A/B/C 评级、Turtle 20/55 突破、SQLite 表结构和报告派生边界，只修复已经由审计证明的业务正确性问题。

---

## 2. 原问题与修改结果

### 2.1 原问题

原实现的正式通知候选为：

```text
首次进入 A 级，或 A 级方向变化
```

真实入场条件却是：

```text
A 级同向趋势
+ 同方向 Turtle 突破
+ 无资格/风险阻断
```

因此存在：

1. **误报**：达到 A 级但没有突破，最终动作是 `WAIT`，仍可能发送 A 级通知；
2. **漏报**：前一日已经是 A 级，今日首次突破，最终动作变为入场，但没有新的 A 级状态事件可通知；
3. **语义过度声明**：资格闸门未执行时仍可能输出 `ENTER_LONG/ENTER_SHORT`；
4. **恢复风险**：旧 staging 制品仍携带 A 级技术事件通知 ID，升级后可能再次进入 outbox。

### 2.2 修改后的权威链路

```text
技术证据事件
SMA / EMA / MACD / Turtle / STRATEGY_GRADE_A_*
        ↓
TrendDecisionResult
WAIT / NEUTRAL / ENTRY_CANDIDATE_* / ENTER_*
        ↓ 仅 ENTER_LONG / ENTER_SHORT
ExecutionDecisionEvent
ENTRY_DECISION_LONG / ENTRY_DECISION_SHORT
        ↓
commit → signal_outbox → deliver
```

### 2.3 动作矩阵

| 条件 | `execution_state` | `reason_code` | 正式执行事件 | 正式通知 |
|---|---|---|---|---|
| 研究模式 | `WAIT` | `OBSERVATION_ONLY` | 无 | 无 |
| 数据门禁未通过 | `WAIT` | `DATA_BLOCKED` | 无 | 无 |
| 筛选证据未就绪 | `WAIT` | `SCREENING_NOT_READY` | 无 | 无 |
| 未形成 A/B 同向趋势 | `NOT_APPLICABLE` | `NO_EXECUTABLE_TREND` | 无 | 无 |
| B 级趋势 | `WAIT` | `GRADE_B_WAIT` | 无 | 无 |
| A 级、无同向突破 | `WAIT` | `TREND_NO_TRIGGER` | 无 | 无 |
| A 级 + 突破、资格阻断 | `WAIT` | `RISK_BLOCKED` | 无 | 无 |
| A 级 + 突破、资格未评估/未确认 | `ENTRY_CANDIDATE_LONG/SHORT` | `ELIGIBILITY_NOT_CONFIRMED` | 无 | 无 |
| A 级 + 同向突破 + 资格确认通过 | `ENTER_LONG/SHORT` | `ENTRY_CONFIRMED` | 1 条 | 1 条，幂等 |

---

## 3. 核心设计

### 3.1 `ExecutionDecisionEvent`

新增无 I/O 领域对象：

```python
ExecutionDecisionEvent
- event_id
- event_key
- event_type
- instrument_id / symbol / timeframe
- as_of / action
- decision_hash
- strategy_version
- decision_rule_version
- dataset_version
- detected_at
- trigger_price / reference_value
- trigger_signal_ids
- reason_code
- metadata
```

事件 ID 只使用稳定业务维度：

```text
strategy_version
+ decision_rule_version
+ instrument_id
+ timeframe
+ as_of
+ action
```

不使用 `run_id` 和重试时间。相同业务决策重跑时生成相同 ID，配合 SQLite `INSERT OR IGNORE` 避免重复信号和重复 outbox。

正式入场事件必须：

- `execution_state` 为 `ENTER_LONG` 或 `ENTER_SHORT`；
- `reason_code=ENTRY_CONFIRMED`；
- `eligibility_status=PASSED`；
- 无 `risk_blocks`；
- 趋势方向与动作方向一致；
- 同一决策 bar 至少存在一个同方向 `TURTLE_20_BREAKOUT` 或 `TURTLE_55_BREAKOUT`；
- 保存全部源突破事件 ID，保证可追溯。

### 3.2 兼容现有 SQLite

`ExecutionDecisionEvent` 被投影为现有 `SignalEvent`：

```text
indicator_name = execution_decision
signal_type = ENTRY_DECISION_LONG | ENTRY_DECISION_SHORT
signal_id = event_id
```

继续写入现有：

```text
signal_events
signal_outbox
```

没有新增表、字段或迁移脚本。

### 3.3 新旧通知选择接口

新接口：

```python
notification_event_ids
```

旧接口名仍保留：

```python
notification_signal_ids
```

兼容边界：

- 旧参数名仍可调用；
- 新旧参数都执行“仅允许确认执行事件”的安全语义；
- 未显式传入 ID 时，Repository 默认只自动选择执行决策事件，不再把全部技术事件放入 outbox；
- 新阶段投影同时写入 `notification_event_ids` 和同值旧别名，方便旧读取端过渡；
- schema v1 staging 中的 A 级通知 ID可恢复读取，但会被安全抑制，不再进入正式 outbox。

### 3.4 资格闸门 fail closed

原默认配置：

```yaml
eligibility_gate_enabled: false
```

现在的明确语义：

```text
false 或无 ExecutionContext
→ ENTRY_CANDIDATE_LONG / ENTRY_CANDIDATE_SHORT
→ 不生成正式执行事件
→ 不通知
```

要得到正式 `ENTER_LONG/ENTER_SHORT`，必须启用资格闸门并注入实际执行上下文，使结果同时满足：

```text
status = PASSED
evaluated = true
passed = true
```

---

## 4. 修改文件清单

### 4.1 根目录与文档

| 路径/文件 | 类型 | 具体修改 | 修改原因 / 解决问题 | 原功能与兼容性影响 |
|---|---|---|---|---|
| `pyproject.toml` | 修改 | 包版本由 `0.2.0` 升级为 `0.2.1` | 标识本次 P0 行为修复版本 | 安装方式、依赖、入口不变 |
| `README.md` | 修改 | 重写为当前交付包真实范围；新增执行事件、候选语义、安装、验证、兼容性和后续边界 | 原 README 引用了包内不存在的历史 tests/docs/scripts，并声明无法独立复核的历史结果 | 文档修正，不改变运行；明确本次仅验证 P0 定向测试 |
| `CORE_CODE_ARCHITECTURE.md` | 修改 | 更新阶段输出、正式通知链路、持久化兼容和设计规则 | 架构文档需要与新业务权威事件一致 | 六阶段边界不变；补充真实语义 |
| `项目代码修改说明_v1.md` | 新增 | 本文档 | 提供统一修改说明、逐文件清单、兼容性与验收信息 | 无运行影响 |

### 4.2 Core 领域层

| 路径/文件 | 类型 | 具体修改 | 修改原因 / 解决问题 | 原功能与兼容性影响 |
|---|---|---|---|---|
| `src/inv_trend/core/decision_events.py` | 新增 | 定义 `ExecutionDecisionEventType`、`ExecutionDecisionEvent`、稳定 ID、身份完整性校验、有限价格校验、源突破追踪及 `SignalEvent` 投影 | 建立独立于 A 级技术事件的正式业务事件 | 新增 API；不修改既有 `SignalEvent` |
| `src/inv_trend/core/__init__.py` | 修改 | 导出执行决策事件模型和默认决策规则版本 | 提供稳定公开导入路径 | 既有导出不变，仅新增 |

### 4.3 Application 模型与决策

| 路径/文件 | 类型 | 具体修改 | 修改原因 / 解决问题 | 原功能与兼容性影响 |
|---|---|---|---|---|
| `src/inv_trend/application/daily_models.py` | 修改 | `TrendDecisionResult` 新增 `reason_code`、`eligibility_status`；schema 升至 v2；schema v1 Hash 保持原算法；报告变更日志新增 P0 修复说明 | 明确动作原因和资格状态，并保证旧阶段恢复可校验 | 新结果 Hash 会按 v2 变化；v1 结果可继续读取和验 Hash |
| `src/inv_trend/application/daily/stage_payloads.py` | 修改 | 反序列化新字段，旧 payload 缺字段时使用兼容默认值 | 支持 v1/v2 阶段结果恢复 | 向后兼容 |
| `src/inv_trend/application/daily/trend_decision.py` | 修改 | 增加 fail-closed 动作矩阵；新增原因码；资格未确认输出候选；从最终确认决策和同 bar Turtle 事件生成 `ExecutionDecisionEvent`；正式通知 ID 改为执行事件 ID；报告投影包含执行事件；投影 schema 升至 v2 | 同时消除 A 级无突破误报和“已为 A 后首次突破”漏报；避免未评估资格被声明为可执行 | 指标、评级和突破算法不变；正式动作语义收紧 |
| `src/inv_trend/application/daily/__init__.py` | 修改 | 导出 `build_execution_decision_event` | 统一应用层公开入口 | 仅新增导出 |
| `src/inv_trend/application/daily/projections.py` | 修改 | 报告映射支持“做多候选/做空候选” | 避免候选被错误显示为“不入场”或正式入场 | 展示增强，不改策略 |
| `src/inv_trend/application/daily/ports.py` | 修改 | Repository 端口新增 `notification_event_ids`；保留旧参数名；通知端口语义改为正式执行事件 | 让应用接口与业务事件一致 | 方法参数向后兼容；无删除 |
| `src/inv_trend/application/daily/signal_commit.py` | 修改 | commit 读取新通知 ID；严格校验执行事件类型；旧 staging A 级通知 ID安全抑制；行级审计记录通知来源和执行事件 | 保证只有最终执行事件可进入 outbox，同时不阻断旧 staging 恢复 | 旧技术通知不再发送；旧结果仍可提交其他信号和 cursor |
| `src/inv_trend/application/daily_pipeline.py` | 修改 | 兼容旧单体路径：同样生成执行事件、只选择执行事件通知、支持候选显示；新增字段后重新计算完整报告 Hash | 防止旧入口绕过新规则；避免追加字段后报告 Hash 与内容不一致 | 旧入口仍可用；结果 Hash 因内容修正而变化 |
| `src/inv_trend/application/strategy_config.py` | 修改 | 明确 eligibility 未确认时只能输出 `ENTRY_CANDIDATE_*` | 固化配置语义，防止默认关闭闸门却输出正式入场 | 配置字段和默认值不变 |

### 4.4 配置

| 路径/文件 | 类型 | 具体修改 | 修改原因 / 解决问题 | 原功能与兼容性影响 |
|---|---|---|---|---|
| `src/inv_trend/config/strategy.yaml` | 修改 | 增加 `eligibility_gate_enabled: false` 的候选/通知说明 | 防止使用者误认为关闭闸门仍会产生正式入场 | 无配置值变化 |
| `src/inv_trend/application/config/strategy.yaml` | 修改 | 对历史兼容配置增加相同说明 | 两份现存 loader 路径语义一致 | 无配置值变化；配置收敛仍属 P1 |

### 4.5 Adapter、持久化与投递

| 路径/文件 | 类型 | 具体修改 | 修改原因 / 解决问题 | 原功能与兼容性影响 |
|---|---|---|---|---|
| `src/inv_trend/adapters/detector/storage/daily_signal_repository.py` | 修改 | 新增 `notification_event_ids`；默认仅选择执行事件；新旧选择器均校验事件类型和 ID 子集；单标的与原子多标的提交统一；保留 SQLite schema | 在最终持久化边界阻止技术事件进入 outbox，避免上游遗漏校验时再次误报 | 表结构不变；旧参数名可用，但不再允许 A 级技术事件通知 |
| `src/inv_trend/adapters/detector/alerts/daily_notifier.py` | 修改 | 文档语义改为正式 outbox 决策事件，保留按事件 ID 去重 | 使通知边界描述与实际一致 | 记录格式和调用签名不变 |
| `src/inv_trend/application/daily/notification_delivery.py` | 修改 | 明确 deliver 只消费 commit 已确认的执行事件 | 防止投递层被误认为负责策略判断 | 重试、恢复、错误记录逻辑不变 |

### 4.6 展示

| 路径/文件 | 类型 | 具体修改 | 修改原因 / 解决问题 | 原功能与兼容性影响 |
|---|---|---|---|---|
| `src/inv_trend/observability/report_renderer.py` | 修改 | 支持候选动作、执行事件、原因码和资格状态中文显示；明确“突破不等于正式入场” | 报告必须区分趋势证据、候选和正式动作，且展示层不重算 | 旧 ReportBundle 仍可显示；新字段缺失时使用默认值 |

### 4.7 测试

| 路径/文件 | 类型 | 具体修改 | 修改原因 / 解决问题 | 原功能与兼容性影响 |
|---|---|---|---|---|
| `tests/test_daily_execution_decision_events.py` | 新增 | 覆盖动作矩阵、误报、漏报、候选抑制、long/short 对称、稳定 ID、身份防篡改、schema v1 Hash、单标的/原子提交幂等、新旧选择器和旧 staging 抑制 | 为本次 P0 行为建立可执行回归证据 | 不进入运行包业务逻辑；无生产副作用 |

---

## 5. 接口变化

### 5.1 `TrendDecisionResult`

新增：

```python
reason_code: str
eligibility_status: str
schema_version: str = "2"
```

兼容规则：

- schema v2：Hash 包含两个新字段；
- schema v1：Hash 仍按旧字段集合计算；
- 旧 JSON 缺字段时分别读取为 `""` 和 `"UNKNOWN"`。

### 5.2 Repository 端口

```python
commit_events_and_cursor(
    ...,
    notification_event_ids: Collection[str] | None = None,
    notification_signal_ids: Collection[str] | None = None,  # 兼容别名
)
```

选择规则：

1. 两个参数同时传入时必须完全相同；
2. ID 必须属于本次 `events`；
3. 正式通知事件必须满足：

```text
indicator_name = execution_decision
signal_type ∈ {ENTRY_DECISION_LONG, ENTRY_DECISION_SHORT}
```

4. 未传选择器时，仅自动选择上述执行事件；
5. `enqueue_notifications=false` 时始终不创建 outbox。

### 5.3 Trend-decision commit projection

新增权威字段：

```json
{
  "schema_version": "2",
  "execution_decision_events": [],
  "notification_event_ids": []
}
```

兼容字段：

```json
{
  "notification_signal_ids": []
}
```

新投影中两者值相同；旧投影只有 `notification_signal_ids` 时，技术评级通知 ID会被抑制。

---

## 6. 原有功能与兼容性结论

| 范围 | 影响 | 结论 |
|---|---|---|
| Donchian/Turtle 20/55 计算 | 无 | 保留 |
| SMA/EMA/MACD/ADX/ATR/Volume 计算 | 无 | 保留 |
| A/B/C 评分权重与阈值 | 无 | 保留为证据层 |
| 数据版本、质量、血缘、Review、Catalog | 无 | 保留 |
| 六阶段工作流 | 无结构变化 | 保留 |
| `commit` 唯一写入边界 | 无 | 保留并强化 |
| JSON/HTML/CSV 派生原则 | 无 | 保留 |
| CLI 名称与参数 | 无 | 兼容 |
| SQLite 表结构 | 无 | 不需要迁移 |
| 旧 `notification_signal_ids` 参数 | 参数名保留，语义收紧 | 兼容调用；禁止技术事件正式通知 |
| schema v1 staged result | 可读取、可验 Hash | 兼容；旧技术通知安全抑制 |
| 默认日报正式通知 | 有行为变化 | eligibility 未实际通过时不再发送，这是有意修复 |
| 报告 Hash | 新报告变化 | 因新增权威字段，属于正确变化 |

---

## 7. 覆盖使用方式

### 7.1 仅覆盖本次修改文件

将“仅本次修改文件覆盖包”解压到原项目根目录，保持目录结构并允许覆盖：

```text
原项目根目录/
├─ pyproject.toml
├─ README.md
├─ CORE_CODE_ARCHITECTURE.md
├─ 项目代码修改说明_v1.md
├─ src/
└─ tests/
```

覆盖前建议备份当前代码。SQLite 不需要迁移，但正式环境仍建议备份状态数据库。

### 7.2 使用完整修改后代码包

完整代码包已经移除：

- `.git/`；
- `__pycache__/`、`.pyc`、`.pyo`；
- `.pytest_cache/`；
- `*.egg-info/`；
- 日志、数据湖、回测输出、临时文件和构建产物。

解压后执行：

```bash
python -m pip install -e ".[test]"
PYTHONPATH=src pytest -q
```

### 7.3 恢复正式入场通知

默认关闭资格闸门时只产生候选。正式部署需要：

1. 将 canonical 配置中的 `trend_decision.eligibility_gate_enabled` 设为 `true`；
2. 注入包含 checker、账户/执行准备度和回测验证状态的 `ExecutionContext`；
3. 确认 eligibility 返回 `PASSED/evaluated=true/passed=true`；
4. 先在研究或影子环境核验 outbox，再启用实际通知通道。

---

## 8. 验证结果

本次在修改工作树执行：

```bash
PYTHONPATH=src pytest -q
PYTHONPATH=src python -m compileall -q src tests
node --check <从 report_renderer.py 提取的内嵌 JavaScript>
```

结果：

| 检查 | 结果 |
|---|---|
| P0 定向回归 | `19 passed` |
| Python 编译 | 通过 |
| HTML 生成 smoke | 通过 |
| 内嵌 JavaScript 语法 | 通过 |
| Git 差异空白检查 | 通过 |
| Wheel 构建与隔离导入 | `inv_trend_trading-0.2.1-py3-none-any.whl` 构建成功；新模块与两份策略 YAML 均已打包 |
| SQLite 单标的重复提交 | 1 条信号、1 条 outbox，重复运行不新增 |
| SQLite 原子 daily commit | 仅确认执行事件进入 outbox |
| schema v1 TrendDecision Hash | 与旧算法一致 |

关键回归场景：

```text
A 但无突破
→ WAIT / TREND_NO_TRIGGER
→ 0 条执行事件，0 条通知

前日已 A，今日首次突破，资格通过
→ ENTER_LONG/SHORT
→ 1 条 ENTRY_DECISION_*，1 条通知

A + 突破，资格未评估
→ ENTRY_CANDIDATE_LONG/SHORT
→ 0 条正式通知

A + 突破，资格阻断
→ WAIT / RISK_BLOCKED
→ 0 条正式通知

重复运行同一决策
→ 稳定 event_id
→ signal_events/outbox 不重复
```

### 未在当前环境完成的验证

当前运行环境未提供完整原始测试资产、固定数据集、Golden expected、PyArrow/DuckDB 真实后端、真实 Provider 响应、Windows 文件系统和多进程故障注入环境。因此本次不宣称：

- 历史 README 中的 `239 passed` 已复现；
- 全量数据治理生产验收已完成；
- 日报策略与多资产回测已经共用同一个策略内核；
- 策略收益有效性已得到验证。

---

## 9. 后续建议

### P1

1. daily、detector、multi-asset backtest 统一调用同一个 `StrategyKernel + DecisionPolicy`；
2. 建立 `FAST_FIRST/SLOW_FIRST` legacy profiles 和 Golden parity；
3. 多份 `strategy.yaml` 收敛为一个 canonical schema；
4. application 只依赖端口，具体 SQLite/notifier 进入 composition root；
5. 对当前 A/B/C + breakout 策略建立逐 bar、成本后、样本外回测。

### P2

1. 清理旧 pipeline、重复配置和兼容 adapter；
2. 拆分大 facade；
3. 完成真实 PyArrow/DuckDB/Provider/Windows/并发/故障注入验收；
4. 只有在简单 baseline 的样本外结果不足时，再研究多期限趋势或 regime 模型。

本次覆盖包不包含上述未实施内容，避免把未来设计伪装为已完成代码。
