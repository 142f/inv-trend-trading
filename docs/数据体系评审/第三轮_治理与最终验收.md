# 第三轮：治理与最终验收

## 实现与 Review

- 引用审计按正式入口、兼容入口、无引用模块分类，不再根据 `_v1`/`_v3` 文件名决定删除。
- `四资产行情_v1.py`、`时点行情_v1.py` 仍有生产引用，继续维护且不加入删除清单。
- Provider 基础合同现在拒绝非数值价格和非法时间戳；跨源比较拒绝 naive 时间、重复时间和非法价格。
- 数据基础设施文档已校正 v2/v3、边界模型和生产验收口径。

## 验收状态

- 定向 pytest：模型与质量门禁 7 项、v3 迁移与存储 44 项通过。
- Ruff：本次新增及核心修改文件通过；存量压缩风格文件仍有既有 lint 债务，未用格式化器批量改写。
- 全量 pytest：系统 Python 因缺少 `rich` 在 3 个模块收集阶段中断；项目 `.venv` 含完整依赖，但其 pytest 9 在 Windows 创建 `tmp_path` 时触发 ACL 权限错误。因此未宣称全量通过。
- PyArrow 单项由项目环境可导入，但真实 Provider/生产数据根未作生产验收。
- 用户已有工作区修改与删除均予以保留。
