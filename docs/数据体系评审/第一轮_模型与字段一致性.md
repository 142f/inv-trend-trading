# 第一轮：模型与字段一致性

## 实现与 Review

- `InstrumentConfig` 保持行情身份权威；detector 和多资产对象只作为边界视图。
- 唯一单向投影位于 `data/契约映射.py`，身份字段完整传递，风险参数必须显式提供。
- Review 发现股票数据层的 `provider_adjusted_close` 比 detector 枚举更细；已在投影边界明确映射为 `back_adjusted`，权威配置不被改写。
- 多资产 CSV 列改名为 `MULTI_ASSET_BAR_COLUMNS`，并提供到持久化 `CANONICAL_COLUMNS` 的显式转换；`spread` 作为扩展列保留。
- 新写入时间统一为带 UTC 偏移的 RFC 3339；naive datetime 被拒绝，旧 `Z` 格式仍可读取。

## 验证与结论

- 模型映射、未知资产、持久化字段、UTC 与日期序列化定向测试通过。
- 第一轮问题已修复，准入第二轮。
