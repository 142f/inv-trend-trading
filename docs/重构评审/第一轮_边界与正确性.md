# 第一轮：边界与正确性评审

## 已完成

- 新增零基础设施依赖的领域契约和 `Strategy` Protocol。
- 新增统一 Bar 事件排序规则，并接入多资产回测器。
- EMA 与海龟信号通过应用层适配器接入统一契约，原算法未改写。
- 增加领域依赖边界、同 Bar 禁止成交及未来数据隔离测试。

## Review 发现与修复

- 首次适配把运行元数据 `symbol` 传入 EMA 参数类型，触发未知参数异常。
- 已将策略参数与运行上下文分离，仅允许声明在 `parameter_schema` 中的参数进入配置。

## 验证

- Ruff：通过。
- 领域契约、架构边界和 EMA 回归：27 项通过。

## 遗留风险与准入结论

- 旧回测入口仍需验证弃用警告和返回值兼容。
- 准入第二轮：通过。
